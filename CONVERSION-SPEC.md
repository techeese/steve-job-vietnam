# CONVERSION-SPEC — Sitting S1 "Lên Đại Học" (v1 HS skeleton → University MVP)

Assumes the v1 skeleton exists and passes its gates (map/sprites/clock/save/events/stats/outcome-cascade per MVP-SPEC.md). This sitting is a CONVERSION DIFF + four v0 systems. Where this file conflicts with anything else, DESIGN.md v2 rulings win; for raw numbers, THIS file wins for S1. All new constants land in CONFIG.ADMIT / CONFIG.ALUM / CONFIG.FUND / CONFIG.PANTHEON.

## 0 · Scope in one paragraph
Reskin the 3-grade high school into a 4-year university (strings + calendar + cohort link), replace the recruiting card-picker with the ĐIỂM CHUẨN modal, convert the June exam into bảo vệ đồ án with the canonical 8-row cascade (🍎 row removed, hidden 🌱 tiemNang flag), and ship four v0 systems: alumni lifecycle (8 states, deterministic, Trần Phi Lợi scripted), funding trio v0 (endowment line + 2 corporate contracts), and 3 pantheon scholarships. One S.v 1→2 migrator covers everything. Estimated ~1,400–1,800 lines net; the trim line in §11 is the honesty mechanism.

## 1 · Exact rename/relabel diff (strings + structure)

| v1 | v2 |
|---|---|
| "Học viện Steve (tên đầy đủ đang chờ Sở duyệt)" | "Học viện Steve (tên đầy đủ đang chờ Bộ duyệt mã trường)"; boot card: bought "Trường ĐH Dân Lập Văn Mẫu Số 9" |
| Sở (everywhere) | Bộ; add "Khoa Công Nghệ & Sáng Tạo (mã ngành đang chờ Bộ duyệt)" subtitle in 📊 + admit modal |
| giáo viên / lớp 10–12 | giảng viên / Năm 1–4 (khóa K1, K2… flavor); "học sinh"→"sinh viên" everywhere |
| 🎓 tab "Học sinh" | "Sinh viên" + segment control [Sinh viên \| Cựu SV] |
| students[].grade 'g10'/'g11'/'g12' | int 1–4 (boot: 1–4; migrator: g10→2, g11→3, g12→4 — mid-degree, Năm 1 empty until first admissions; never invent students) |
| presets {g10,g11,g12} | {n1,n2,n3,n4}; keys 'luyende'/'canbang'/'duan' UNCHANGED (save compat); labels: Lò Luyện Đề→"Học Để Qua Môn", Cân Bằng, Dự Án→"Đồ Án & Lab"; numbers untouched |
| Roster cap 36 | 48 (CONFIG.quotaCap; sanitize slice(0,48)) |
| Rivals: iSteve / THPT Bán Công Bình Thường / Trường Làng Bến Sung | Học viện iSteve Toàn Cầu ("Cam kết đầu ra: Kỳ lân hoặc hoàn 30% học phí") / ĐH Dân Lập Bình Thường / ĐH Cộng Đồng Bến Sung |
| Recruiting modal (card-picker, pool 6+(4−rank)×2) | #admitModal (cutoff+quota dials, §4); v1 card COMPONENT survives for tuyển thẳng (S2) + spotlight (§4f) |
| June exam (điểm = 2+0.06KT+0.02Vẹt) | Bảo vệ đồ án (điểm = 2+0.045TN+0.035ST+0.02Vẹt, §3); policies relabeled ĐỒ ÁN MẪU / BẢO VỆ THẬT (same numbers) |
| Rank from June score avg | Rank from declared cutoff vs rival curves, lands Tháng 8 (§4e); payouts +8/+4/+1/−3 unchanged |
| essayQ | defenseQ (rename; formula 0.5ST+0.3TN+0.2UT, viral ≥70 → +10 TT +5 UT unchanged) |
| Cascade 6 rows incl. 🍎 | 8 rows, 🍎 removed (§3); "TIỀM NĂNG:" header; entry-state footer line |
| sponsor:{offered,accepted,refusals} | contracts[] + corpBlacklist + offersSeen (§6; migrator §7) |
| scriptedFired.coinScandal (modal) | CONFIG.ALUM.SCRIPTED row for TPL (§5d); generic arrest modal |
| Term report THU (one blob) | 3 rows: 🎓 học phí · 🤝 hợp đồng · 🌱 quỹ lãi (§6c); modal retitled "Sơ kết học kỳ" |
| Speeds pause/1×/2× | + 3× (ticks-per-interval=3), speed3Unlocked flips at first Lễ Tốt Nghiệp (June Y1) |
| End-of-arc summary June Y3 | June Y4 (constant change; + alumni table column "trạng thái") |
| Event deck texts | Re-skin pass per DESIGN v2 §12 (text/predicate only; event 8 → "chợ đồ án mẫu" — same payload: 15tr, +0.5 điểm, +10 Vẹt, seed sev3) |
| 12-event no-repeat, Phốt v0, tells, placement, Tết, autosave | UNCHANGED — do not touch |

## 2 · Boot roster & calendar
Boot: cash 120tr · book 150tr · quỹ 10tr · tuition 2.0 · TT 25 · UT 10 · TC 30 · S.admissions.lastCutoff 15.0 (inherited vét-đáy) · 42 SV: Năm 1 (12, yours: KT 15–30, TN 5–20, ST 15–35, CM 5–20, Mood 65–80, Vẹt 0–10; **Mai Sương** fixed: seed 5, ST 35, tell 'sky') · Năm 2 (10: KT 40–60, Vẹt 30–50) · Năm 3 (10: KT 50–70, Vẹt 40–60, Mood 40–55) · Năm 4 (10 đồ-án-mẫu zombies: KT 65–85, TN 5–15, ST 5–20, Vẹt 60–85, Mood 35–50; **Trần Phi Lợi** fixed: KT 55, TN 25, CM 65, Vẹt 80). Giảng viên 3 inherited: Thầy Thể Dục Kiêm Mọi Thứ (2/2, 18tr) · Cô Giáo Trình Mẫu (2/9, 22tr, biên chế) · **GS Đếm Bài Báo ISI (3/5, 28tr, biên chế — unfireable; +0.5 TT/th aura; his assigned cohort gains −10%)**. Hire pool: v1's four, re-titled giảng viên. Y1 honest net ≈ 84−68−1.05−materials ≈ +13..15tr/th (band +8..25 ✓; sweep-verify, nudge bảo trì not tuition).
Calendar remap per DESIGN v2 §4: Năm-4 đồ án mode Tháng 2–5 (chiều schedule → Xưởng/Phòng Máy cosmetic; defense −1.0 if neither built); June two-stage; scholarship draws Tháng 9 day 1; 20/11 gift flush; cohort roll in June (one extra link: 3→4), matriculation Tháng 9 day 1.

## 3 · June conversion (ceremony modal reused, constants swapped)
Beats: (1) đề pool text + foreshadow "{N} hồ sơ đang hướng về cổng trường" (N from next pool formula, preview only). (2) Policy: ĐỒ ÁN MẪU (+1.0 all, +5 Vẹt cohort, +1 seed sev1, coin-flip +4 TT / "43 slide giống nhau từng hiệu ứng chuyển trang" −6 TT) vs BẢO VỆ THẬT (raw, viral ×2). (3) Per Năm-4: điểm = clamp(2 + 0.045×TN + 0.035×ST + 0.02×Vẹt + policy + (Xưởng∨PhòngMáy ? 0 : −1.0) + 0.1×min(5, QVM_alumni) + rnd(±0.5), 0, 10); đỗ ≥5.0. NO rank here. (4) Mirror: line banks re-costumed (bank A ×3 identical "Trong dòng chảy bất tận của cách mạng công nghiệp 4.0…" at cohort Vẹt≥60; "con ốc M2 toét ren" as defense answer); defenseQ ≥70 → viral 30%/60%: +10 TT +5 UT pierce. (5) Graduation flips — **cascade per DESIGN v2 §7 table (8 rows, priorities exactly as listed)**; card header "TIỀM NĂNG: {outcome}"; footer line "Trạng thái đầu đời: {entry chip}"; near-miss line unchanged; **tiemNang** = ST≥80∧TN≥70∧CM≥60∧Vẹt≤50∧TC≥55 → flags.tiemNang=true + ONE quiet line: Bác Tâm: "Đứa này… để xem." (no meter moves, no confetti); grat frozen = clamp(0.35×mood + 0.35×(100−vet) + 8×flags.vt.length + 10×(hb?1:0)×10…, 0, 100) — implement as 0.35mood+0.35(100−vet)+8×vtCount+10×hbTier, hbTier∈{0,1}; full stat block → alumni record (§5a). (6) Headline unchanged. Speed3Unlocked=true after the first ceremony. v0 entry-state mapping for the two states not yet shipped: Xuất Ngoại card → state 'KY_SU' + flags.xn=true; Reviewer card → 'LUONG_ON' + flags.kol=true (S3 migrates them to XUAT_NGOAI/KOL from stored outcome).

## 4 · Admissions v0
**(a) Pool (Tháng 7 day 1):** S.admissions.poolSeed = (rnd()*2**32)>>>0 (one draw from main stream, then independent). Applicant i derives from r_i = mulberry32((poolSeed ^ Math.imul(i+1, 0x9E3779B9))>>>0), FIXED draw order: gaussScore → ưuTiên tier → (reserved: họcBạĐẹp flag, S2) → seed weights → vet0 → kt0 → tn0 → st0 → cm0 → mood → tell → name. Formulas verbatim from DESIGN v2 §5a–b. N = round(24+1.6×TT) clamp 24–180. Pool NEVER serialized; `__test.pool()` re-derives.
**(b) Modal #admitModal (full-screen, auto-pause, Tháng 7 day 1):** header 48px "TUYỂN SINH · Năm {Y}" / khoa subtitle / badge ĐỢT 1 + anchor line "Năm ngoái: {lastCutoff} — {lastFill}/{lastQuota} nhập học"; context line (N count-up ~1s); histogram 19 bins (12→30+), CSS divs 17px bars + 2px gaps centered in 374px, gold tint at/above cutoff, rival ▼ markers at iSteve/BT/BS curve values, red dashed cutoff line + 28×28 drag handle (44×44 hit), snap 0.25; dial row A ĐIỂM CHUẨN [−][24.25][+] (12.00–30.50, 0.25); dial row B CHỈ TIÊU [−][12][+] (4..min(14, 48−roster)); DỰ BÁO card: L1 "Đủ điểm: {M} → Trúng tuyển {min(M,quota)}/{quota}" + crowding suffix "(sĩ số {n} — hiệu quả −{(1−crowd)×100}%)" when n>12 + amber "thiếu {d}" chip when M<quota; L3 "Học phí: +{n×tuition}tr/tháng (~{tuition×40}tr/suất trọn khóa)"; Lrank "Dự kiến hạng {r}/4 trên BXH điểm chuẩn" (declared vs rival curves); L5 risk lines when armed: "⚠ Nguy cơ phốt: thủ khoa vẫn trượt" (≥29.5) / "⚠ Báo sẽ gọi đây là vét sàn" (≤15.5); CTA 52px "📣 CÔNG BỐ ĐIỂM CHUẨN" → con-dấu "ĐÃ DUYỆT" stamp; footer "Quyết định này lên báo. Theo cả hai nghĩa." Touch ≥44px; modal scrolls, map never.
**(c) Stunt reactions (fire on công bố):** ≥29.50 → +8 TT, −2 UT, seed{src:'admit295',sev:1}, ticker "Thủ khoa 29,9 điểm vẫn trượt vì tiêu chí phụ" · ≥30.00 → +12 TT, −3 UT, seed sev2, "ĐIỂM CHUẨN 30,5/30 — ba điểm 10 vẫn trượt nguyện vọng 1" · ≥27 → +4 TT "cao kỷ lục" · ≤15.50 → −3 TT, cohort vet0 +10 bias, iSteve mock line. (UT deltas legal under the scandal-penalty precedent; net-cap accounting applies.)
**(d) Resolution (Tháng 8 day 1):** M = count(score≥cutoff); enroll top-min(M,quota); NO ảo, NO bổ sung at v0 (S2) — shortfall = empty seats + diary line. Crowding: if cohort n>12 → gains ×max(0.6, 1−0.02×(n−12)) + Mood −1/th (flag on cohort). **BXH:** rivals' cutoffs = iSteve 26.5+0.5×(year−1)±0.75, BT 19±1, BS 15.0 (noise from rnd(), persisted in examHistory row {year, cutoff, rivals[3], rank, fill}); rank payouts TT +8/+4/+1/−3 + ceiling bonus rank≤2; surfaced as TOAST + ladder strip in 📊 (no modal). Store lastCutoff/lastQuota/lastFill; push declaredHistory.
**(e) Matriculation (Tháng 9 day 1):** enrolled applicants instantiated as students (grade 1, tier flags, scholarship award §8), walk in at cổng; **spotlight: 3 cards** (top score · scholarship holder or random · random with visible tell) via the v1 applicant-card component in a small non-pausing sheet, "TÂN SINH VIÊN K{n}".
**(f) Auto-resolve** (modal unconfirmed by Tháng 7 day 28, or headless): cutoff = max(15, lastCutoff−0.5), quota 12, diary "Phòng Đào Tạo tự quyết vì hiệu trưởng đi vắng."

## 5 · Alumni lifecycle v0
**(a) Record (written at every graduation, incl. June Y1 zombies):** {id, ten, gradYear, outcome, state, yearsInState:0, annMonth, fs:{kt,tn,st,cm,vet,seed}, grat, gifts:0, flags:{tiemNang, coinPath, garage:false, xn, kol, vt:[…]}}. annMonth = [1,2,3,4,5,7,8,9,10,11,12][hash(id)%11] — June structurally excluded. Frozen derived: craft=0.6tn+0.4st, hustle=cm, hollow=vet, lửa=seed (compute on the fly from fs, don't store).
**(b) v0 STATE enum (8):** THAT_NGHIEP, LUONG_ON, KY_SU, FOUNDER, CA_MAP_COIN, QUAN_VAN_MAU, BI_BAT, STEVE. Chips per DESIGN v2 §8.
**(c) Tick:** dayTick day-1-of-month → alumniTick(month): for each a with annMonth===month && !flags.employed: r = mulberry32((S.seed0 ^ Math.imul(a.id,2654435761) ^ Math.imul(S.year,40503))>>>0); draws in FIXED order: (1) Steve/garage roll — ALWAYS drawn, discarded unless state∈{FOUNDER} (GIAM_DOC ×0.5 arrives S3); (2) transition; (3) flavor-line index; (4) gift roll. CONFIG.ALUM.FSM v0 rows (base weights; gates as multipliers 0/1; normalize: if Σ>0.95 scale row to 0.95; clamp each ≤0.95): THAT_NGHIEP→[LUONG_ON .30][KY_SU .10×(craft≥50)][FOUNDER .04×(lửa≥3)] · LUONG_ON→[THAT_NGHIEP .04][KY_SU .08×(craft≥55)][FOUNDER .03×(lửa≥3∧hustle≥50)] · KY_SU→[FOUNDER .06×(lửa≥3)][LUONG_ON .03] · FOUNDER→[THAT_NGHIEP .12][KY_SU .08][CA_MAP_COIN .08×(hollow≥50∧hustle≥60)×(coinPath&&yearsSinceGrad≤2 ? 4 : 1)] · CA_MAP_COIN→[BI_BAT min(.95, .18+.06×yearsInState)] · QUAN_VAN_MAU→[LUONG_ON .03] · BI_BAT→(yearsInState≥2: [THAT_NGHIEP .90][CA_MAP_COIN .10]) · STEVE→absorbing. World multipliers = 1.0 at v0 (S3).
**(d) Steve pipeline:** in FOUNDER: pSteve = 0.05 × (craft≥75) × {seed5:1.5, 4:1.2, 3:0.7, else .25} × (hollow≤40) × (1+0.1×min(5,yearsInState)) × (tiemNang?1:0.15). First success → garage=true, state locked FOUNDER this year, −2 TT, ticker "Công ty của {tên} sa thải 90% nhân sự. {tên} đang ngủ trong ga-ra." Next year roll ×3: success → STEVE (reuse the v1 keynote parody screen verbatim; +15 TT, +5 UT pierce, META.jobsEver, mega-gift 500tr→quỹ); fail → KY_SU (the 50/50 with QUAY_DAU arrives S3), garage stays true (flag preserved). Sweep target: median first keynote year 7–9.
**(e) Trần Phi Lợi scripted (CONFIG.ALUM.SCRIPTED = [{ten:'Trần Phi Lợi', entry:'CA_MAP_COIN', forceYear:2, force:'BI_BAT'}]):** entry forced at June Y1 graduation; Tháng 3 năm 2 → forced BI_BAT through the GENERIC arrest modal: "Cựu sinh viên Trần Phi Lợi bị bắt — sàn coin XÔIĐỖ sập", TT −15×(1+TT/50), UT −2, line "khóa này tốt nghiệp thời chủ cũ"; if any contract live → morality-clause loop runs (the demo). Replaces scriptedFired.coinScandal's modal; field retained as the fired-flag.
**(f) BI_BAT entry (any alumnus):** guaranteed detonation that year: arrest modal, damage = 15×(1+TT/50)×max(0.5, 1−0.05×(S.year−gradYear)), UT −2.
**(g) Gifts v0:** stateBase {KY_SU .20, LUONG_ON .02, QUAN_VAN_MAU .05, STEVE 1.0, others 0}; p = clamp(base×grat/50, 0, .95); amount: KY_SU 10–30tr, LUONG_ON 2–5tr, QVM 5–10tr, STEVE 500tr; × (1+UT/100). Routing: <30tr → cash "quà cảm ơn" toast; ≥30tr → endow.bal "hiến tặng". ALL queue to S.endow.pending, flush 20/11: toast stack "🎓 {n} phong bì từ cựu sinh viên" + one Bác Tâm nod + letter modal for the largest (quote: if a.flags.vt.length → keyed bank ('tuaVit' → "…chuyện cái tua vít."), else generic bank headed by "Cảm ơn thầy đã không bắt em học thuộc."). No confetti, no coin sound. Scripted guarantee: if zero gifts by 20/11 Y2 and ≥1 KY_SU alumnus exists → force one 30tr gift (the v1 almLetter beat, preserved).
**(h) virtueTouches:** student.flags.vt = array of event keys, appended on kindly resolutions (tuaVit, phongMáy, pccc, hocBong, …). v1 boolean virtueFlags object retained for predicates.
**(i) Sổ v0:** segment [Sinh viên | Cựu SV] in 🎓 tab; rows ≥44px: thumb+grad-cap, "tên · K{gradYear}", state chip (gold STEVE pinned top, red BI_BAT), last line ellipsis; slice 30 + "Xem thêm"; red-dot only for: garage, BI_BAT, STEVE, or transitions of alumni with tiemNang∨tell∨vt.length≥1. Detail card: portrait + year→chip timeline + gifts total + replayed graduation card. (Filters, gratitude flavor tiers → S3.)
**(j) Ticker budget:** ≤2 alumni lines/month, per-state banks 2–3 variants each (deadpan, no exclamation marks), overflow → eventLog.

## 6 · Funding v0
**(a) Endowment:** S.endow = {bal:10, log:[], pending:[], drawnYear:false, milestonesClaimed:0}. Month boundary: bal = bal×1.004; grey ticker line monthly "Quỹ hiến tặng: {bal}tr (+{lãi}tr lãi)" (counts against no budget; one line). CONFIG.FUND = {ENDOW_RATE:0.004, GIFT_TO_QUY_MIN:30, SCHOL_GATES:[200,350,500], SCHOL_DRAW:12, CONTRACT_CAP:()=>Math.min(3,1+Math.floor(S.tiengTam/35)), MORALITY:sev=>0.5+0.25×sev, BREAK:{feeMonths:1, tt:−8, blacklistYears:2}}. No HUD pixel until bal≥200tr (then grey 🌱 beside 💰). No spend channels at v0 except scholarships (§8) — Ứng Cứu/dedications S4/S5.
**(b) Contracts (2 offers in S1):** schema {id,co,type,pay,signBonus,mLeft,strings:[],strikes:0}. OFFER 1 (scripted, minute ~2:30, replaces v1 sponsor modal — same timestamp, ~90% code reuse): Tập đoàn Trứng Vàng, type 'pr', 12tr/th + 30tr signing, 10 months, strings ['pr']: 3 days/th top-3 (ST+TN) students rehearse — v1 rendered-cost path VERBATIM (gains zeroed, motes stopped) — +2 TT/th, TC −2 on sign, seed none; refuse → +2 TC, +1 UT, virtue flag, nod (v1 numbers identical). OFFER 2 (term boundary Tháng 12 năm 2, predicate: Phòng Máy∨Xưởng built): Cty CP Rất Minh Bạch, type 'lab', 60tr/th, 24 months, sign TC −5 + seed{src:'lab',sev:2}; string: the room gets fence+logo overlay (one sprite), its effects SUSPENDED (Dự Án reverts to no-room −50%; Xưởng bonuses off) — the deal switches off your thực-chất engine, rendered. If cap full → modal offers [Phá vỡ hợp đồng cũ trước] with full penalty preview. Break: pay 1×monthly fee, −8 TT, corpBlacklist[co]=year+2, +2 TC if broken contract had Vẹt/lock strings. Morality clause: on ANY phốt detonation, each live contract rolls MORALITY(sev) → instant cancel, ticker line. Payouts + mLeft−− at month boundary; expiry → silent end (renewal escalation S4).
**(c) UI:** #moneyVal → Tài chính bottom sheet (≤62vh): 💰 cash / THU: "🎓 Học phí {n} sv × {t}tr → {x}tr" · "🤝 Hợp đồng {k} đang chạy → {y}tr ▸" (rows: logo chip, pay, mLeft, string chips, [Phá vỡ] + penalty preview; tapping a string chip names the cost: "Đang trả bằng: 3 ngày/tháng của {top3 names}") · "🌱 Quỹ hiến tặng {bal}tr · +0,4% → {lãi}tr ▸" (detail: sparkline 12 points, gift log, scholarship plaque cards) / CHI: lương · bảo trì · vật tư. Term report THU = the same 3 rows; kế-toán line "Trần học phí hiện tại: {x}tr. Điểm chuẩn hàng xóm: {iSteve}." 📊 Trường gains a contract-count chip.

## 7 · Save model (S.v 1→2, ONE migrator)
**freshState() additions/changes (every field: default + typeof-merge + sanitize):**
```js
v:2, seed0:0,                       // boot: seed0 = initial rngState >>> 0
students:[…grade:1..4…, flags:{…,vt:[]}], // cap 48
presets:{n1:'canbang',n2:'luyende',n3:'luyende',n4:'luyende'},
speed3Unlocked:false,               // speed ∈ {0,1,2,3}
admissions:{poolSeed:0,lastCutoff:15,lastQuota:12,lastFill:0,aoCount:0,bonusOffered:false,declaredHistory:[]},
endow:{bal:10,log:[],pending:[],drawnYear:false,milestonesClaimed:0},
scholarships:[{key:'tdn',holderId:null,suspended:false},{key:'tqb',holderId:null,suspended:false},{key:'hxh',holderId:null,suspended:false}],
contracts:[], corpBlacklist:{}, offersSeen:[],
alumni:[/* full records per §5a */]
// REMOVED: sponsor{} (migrated), speed2Unlocked kept as-is
```
**Migrator (runs when loaded.v===1):** grade 'g10'→2,'g11'→3,'g12'→4; presets {n1:'canbang', n2:old.g10, n3:old.g11, n4:old.g12}; sponsor.accepted → contracts=[{id:1,co:'trungvang',type:'pr',pay:12,signBonus:0,mLeft:10,strings:['pr'],strikes:0}]; sponsor.refusals → virtueFlags (kept); alumni v1 rows {id,ten,outcome,year} → + gradYear:year, state:entryMap(outcome) ('jobs'→'STEVE','camap'→'CA_MAP_COIN','vanmau'→'QUAN_VAN_MAU','kysu'→'KY_SU','reviewer'→'LUONG_ON'+kol,'luongon'→'LUONG_ON'), yearsInState:max(0,S.year−year), annMonth:derived, fs:outcome-template table (kysu:{kt:55,tn:75,st:45,cm:50,vet:30,seed:3} etc. — one CONFIG table), grat:50, gifts:0, flags:{}; seed0 = rngState>>>0; v=2.
**sanitize() additions (bad = v=>!Number.isFinite(v) everywhere):** grade int clamp 1–4; students slice(0,48); presets keys ∈ {luyende,canbang,duan} else 'canbang'; admissions: poolSeed>>>0, lastCutoff clamp 12–30.5, lastQuota int 4–14, lastFill/aoCount int ≥0, declaredHistory slice(0,20) rows finite; endow.bal finite ≥0, log slice(0,100), pending rows {amt finite, alumId, key string}; scholarships rebuilt against the 3-key registry (unknown keys dropped, missing keys re-added; holderId must exist in students else null); contracts filtered vs CO/TYPE registries, pay finite, mLeft int 0–48, strikes int 0–3, slice(0,3); corpBlacklist values int; alumni: state ∈ STATE_KEYS else 'LUONG_ON', yearsInState int ≥0, annMonth ∈ 1–12 ∧ ≠6 else recompute, fs fields finite clamp 0–100 (seed 1–5), grat clamp 0–100, gifts finite ≥0; seed0>>>0; speed ∈ 0–3.

## 8 · Pantheon scholarships (3)
CONFIG.PANTHEON = [{key:'tdn', name:'Học bổng Trần Đại Nghĩa', line:'Bỏ lương kỹ sư ở Paris để về nước năm 1946; cha đẻ ngành quân giới — ông tạo ra phương tiện từ sự thiếu thốn.', eff:'tnMult', val:1.15}, {key:'tqb', name:'Học bổng Tạ Quang Bửu', line:'Vị bộ trưởng đại học tự học mọi thứ, và ký giấy cho cả một thế hệ ra thế giới học về.', eff:'stMult', val:1.15}, {key:'hxh', name:'Học bổng Hồ Xuân Hương', line:'Bà chúa thơ Nôm — viết bằng tiếng của chính mình giữa một thời đại viết bằng điển cố đi mượn.', eff:'vetMult', val:0.5}]. Effects are three one-line hooks in the growth pipeline (TN gain ×, ST gain ×, Vẹt accrual ×) applied when student.flags.hb===key. Endow action: quỹ detail shows 3 plaque cards (`.pantheon` CSS class: lacquer-red/gold, serif, 🏵️, the legacy line, NO jokes — enforced in review); button enabled at bal ≥ SCHOL_GATES[i] (200/350/500tr, order endowed); endowing is free, commits the annual draw. Draw Tháng 9 day 1: bal −12tr per active scholarship; if bal <12 → suspended=true this year, −1 UT, quiet ticker "Học bổng {name} tạm dừng một năm. Không có thông cáo báo chí." Award at next Tháng 7: auto-suggest highest-score above-cutoff applicant WITH a tell; one confirm row in the admit modal forecast area (🏵️ badge); holder: tuition 0, seed +1 bias (cap 5) at enrollment, flags.hb=key, hbTier feeds grat (+10). Tone guards: name never in any scandal/phốt headline string (grep-able rule); one reverent tooltip line; hollow-graduating holder (Vẹt≥60) draws ONLY a Bác Tâm diary line.

## 9 · __test & gates
__test gains: `admit:(cutoff,quota)=>declareAndResolve(...)`, `pool:()=>derivedPoolArray`, `alum:()=>S.alumni`, `almYears:n=>fastForwardAlumni(n)`, `endow:n=>{S.endow.bal=n}`. DOM mirrors add `#endowVal` (always the integer bal). gate.sh:
- **GATE_FRESH:** boot ?seed=123 → place('cangtin',6,5) → days(400) → title `GATE_FRESH MONEY=<finite> YEAR=2 STU=<38..48> ALUM=<8..12> ENDOW=<finite>`; crosses THPT-June, đồ án, graduation (8-row cascade), auto-resolved admissions, BXH, matriculation, ≥3 alumni ticks — headless, no JSERR.
- **GATE_ADMIT (new):** boot → days to Tháng 7 → __test.admit(29.75, 6) → assert: photSeeds grew by 1 (src 'admit295'), fill ≤6, roster ≤48 after Tháng 9, AND `__test.pool()` called twice returns identical JSON (poolSeed determinism); then admit at max quota + verify slice(0,48) never triggers (cap arithmetic, not deletion).
- **GATE_ALUM (new, the determinism gate):** python-seed a year-3 save with 10 alumni (mixed states incl. TPL post-arrest) → __test.almYears(5) → snapshot JSON.stringify(S.alumni); reload the SAME save → almYears(5) → byte-identical. Plus assert TPL: fresh ?seed boot → days(1080) → TPL state==='BI_BAT'∨'THAT_NGHIEP', arrest fired flag true, endow.bal>10, ≥1 gift in log.
- **GATE_COMPAT:** python-seed a v1-shaped save (grades g10/g11/g12, sponsor.accepted=true, 30 students, 2 photSeeds, v1 alumni rows) → boot → grades ∈ {2,3,4}, presets n1–n4 present, contracts[0].co==='trungvang', alumni rows have state/grat/fs, days(30) clean.
- **GATE_BUILD:** unchanged.
- **390px screenshots:** #admitModal (with histogram + dials), Tài chính sheet, Sổ Cựu SV list, one `.pantheon` plaque card.
engine.js mirror gains CONFIG.ADMIT/ALUM/FUND + the FSM walker for sweeps. Sweep checklist before ship: Y1 net +8..25 · quota-12 cutoff lands 20.0–21.5 at TT25/UT10, cutoff 24 fills ≤4 · detox pipeline allows seed-5 Vẹt-45 admit → Vẹt≤60∧ST≥90 by June Y4 on Đồ Án & Lab · 1k-decade FSM run: median keynote Y7–9, virtuous-decade keynote 45–60%, hollow <2% · 10k-roll row-normalization fuzz (Σ≤0.95) · 30/30 stunt EV ≈ 60–80% of its TT payout.

## 10 · Deferred (explicitly NOT this sitting)
Ảo + đợt bổ sung, tuyển thẳng strip, đặc-cách/16,5 merged card, học-bạ-đẹp ticker, CLC reflavor, iSteve exposé (→S2). States TRUONG_PHONG/GIAM_DOC/XUAT_NGOAI/KOL/VE_QUE/GIANG_VIEN/QUAY_DAU, world years, tainted alumni offers, 20/11 envelope sprites, Sổ filters/flavor tiers, epilogue, "Mời cựu SV" verb (→S3). Contract types 🦜🎣🤝, renewal escalation, decks T/E/C, Quỹ Ứng Cứu (→S4). Dedications, prizes, remaining 6 scholarships, archetype events, uni teacher hires beyond the inherited ISI prof, new rooms, khoa split (→S5/S6). v1 stubs stay stubbed.

## 11 · Trim line (if the sitting overruns, cut IN THIS ORDER)
1. Sổ detail card → list only. 2. Khai giảng spotlight cards → plain toast. 3. Forecast Lrank → drop (keep L1+L3+L5). 4. Forecast L5 + the four stunt reactions TOGETHER (never ship stunts without the warning — informed gamble; GATE_ADMIT drops its seed assertion to fill+determinism). 5. Histogram → numeric readout "{N} hồ sơ · {M} đủ điểm" (keep dials + drag dies; histogram is S2's first task). 6. OFFER 2 (🔒) → ship 📣 only. 7. HXH scholarship → ship TĐN+TQB (registry sanitize adjusts to 2 keys). 8. 20/11 pending queue → gifts land on annMonth directly (keep routing + quotes).
**NEVER cut:** 4-cohort boot + cap 48 + the v2 migrator; cutoff+quota dials with deterministic pool + auto-resolve; June two-stage with the 8-row cascade + tiemNang + entry-state line + near-miss; alumni FSM 8 states + seed0 isolation + GATE_ALUM + Trần Phi Lợi scripted arrest; endowment line + ×1.004 tick + 3-row term report; speed 3×; save/sanitize discipline; gates green; 390px pass.